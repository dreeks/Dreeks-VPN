version: '3'
services:
  openvpn:
    cap_add:
      - NET_ADMIN
    build:
      context: .
    container_name: openvpn
    ports:
      - "1194:1194/udp"
    restart: always
    volumes:
      - ./openvpn:/etc/openvpn
      - ./hosts:/etc/hosts
